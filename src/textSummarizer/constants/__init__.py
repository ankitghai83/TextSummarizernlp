## This file is used to store the return of config.yaml file & param.yaml file for reading and for that we need path as i don't want to hardcode
from pathlib import Path

CONFIG_FILE_PATH = Path(r"config/config.yaml")
PARAM_FILE_PATH = Path(r"params.yaml")